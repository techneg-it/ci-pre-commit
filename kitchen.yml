---
driver:
  name: docker

provisioner:
  name: dummy

verifier:
  name: inspec

platforms:
  - name: test
    driver:
      image: dafyddj/ci-pre-commit:local-test
      platform: debian

suites:
  - name: default
